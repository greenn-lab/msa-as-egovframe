server.port: 8888

spring:
  application.name: "config-server"

  profiles.active: "native" # required "native" keyword!

management:
  endpoints:
    web:
      exposure:
        include:
          - "env"
          - "refresh"
