server.port: 8888

spring:
  application.name: "config-server"

#  profiles.active: "native" # required "native" keyword!

  cloud:
    config:
      server:
        git:
          uri: "https://github.com/greenn-lab/xx"


  rabbitmq:
    host: "localhost"
    port: 5672
    username: "cc"
    password: "cc"


management:
  endpoints:
    web:
      exposure:
        include:
          - "env"
